allprojects {
    group = "dev.hikari"
    version = "1.0.0"
    description = "TODO"

    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
        maven { url = uri("https://maven.pkg.jetbrains.space/public/p/compose/dev") }
    }
}